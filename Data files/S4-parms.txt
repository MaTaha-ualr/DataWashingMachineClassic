#######################################
# Parameter File for Sample S4G
#######################################
# inputFileName must a valid path to file of entity references
# No default value, this parameter must be specified
inputFileName=S4G.txt
# delimiter the field sparator character, must be comma, 
# semi-colon, colon, pipe character, or tab character. 
# Default value is comma
delimiter=,
# hasHeader must be True or False
# If True, the first line of the file is not processed
# If False, all lines of the file are processed as references
# Default value is True
hasHeader=True
# tokenizerType must be 'Compress' or 'Splitter'
# Compress replaces non-word characters with null
# Splitter replaces non-word characters with space
# Default value is "Splitter"
tokenizerType=Splitter
# truthFileName Optional Parameter
# if provided, must be an existing text file in valid format
# Default value null
truthFileName=truthABCgoodDQ.txt
# runIterationProfile Optional Parameter
# if True, outputs cluster profile & F-meas for each iteration
# Default value False
runIterationProfile=False
# addRefsToLinkIndex Optional Parameter
# If True, the complete reference is included in the link index
# If False, the link index only includes the refID and clusterID
# Default value False
addRefsToLinkIndex=False
########################################
# Global Correction Parameters (OPTIONAL)
# runGlobalCorrection must True or False
# If True, global correction will run prior to blocking
# Default value False
runGlobalCorrection=True
# Global Correction Details (OPTIONAL)
# globalCorrectionDetail must True or False
# If True, global correctiond will print to log file
# Default value False
globalCorrectionDetail=True
# Learn/update token variant map
learnTokenVariants=False
# minFreqStdToken must integer value > 0
# specifies the lowest freq of correction token
# Default value 5
minFreqStdToken=5
# minLenStdToken must integer value > 0
# specifies the minimum length of a correction token
# Default value 3
minLenStdToken=3
# maxFreqErrToken must integer value > 0 and < minFreqStdToken
# specifies the minimum length of a correction token
# Default value 3
maxFreqErrToken=3
########################################
# Block Correction Parameters (OPTIONAL}
# blockCorrection must be True or False
# indicates if block corrections will occur
# Default value is False
blockCorrection=False
# Block Correction Detail (OPTIONAL}
# blockCorrectionDetail must be True or False
# indicates if block correction details are written to logFile
# Default value is False
blockCorrectionDetail=False
########################################	
# Blocking Parameters
# beta must be >= 2 and < sigma
# indicates the maximum frequency of a blocking token
# Default value is 2
beta=8
# blockByPairs must be True or False
# If True, 2 refs must share same 2 tokens to be in same block
# If False, only have to share 1 token to be in same block
# Default value=True
blockByPairs=False
# blockingMode must be 0 or 1
# 0 = existing token-based blocking logic
# 1 = embedding-based blocking using name/address split
# Default value 0
blockingMode=1
# embedding model used when blockingMode=1
embeddingModelName=BAAI/bge-m3
# embedding blocking method: DBSCAN or KNN
embeddingBlockMethod=KNN
# DBSCAN epsilon (cosine distance) for embedding blocking
# Used only when embeddingBlockMethod=DBSCAN
embeddingBlockEps=0.08
# minimum samples per DBSCAN cluster (2 allows lone pairs)
# Used only when embeddingBlockMethod=DBSCAN
embeddingBlockMinSamples=2
# top K neighbors per record when embeddingBlockMethod=KNN
# Used only when embeddingBlockMethod=KNN
embeddingTopK=10
# embedding model batch size
embeddingBatchSize=64
# name embedding weight (address weight is 1.0 - embeddingNameWeight)
embeddingNameWeight=0.76
# embedding device: auto, cpu, cuda, cuda:0, etc.
embeddingDevice=auto
# minBlkTokenLen must be integer value > 0
# min length of a token to use for blocking
# Default value 4
minBlkTokenLen=3
# excludeNumericBlocks must be True or False
# If True, all digit tokens are not used for blocking
# Default value True
excludeNumericBlocks=False
########################################
# Stop Word Parameters
# sigma must be an integer value > beta
# all tokens with freq > sigma are removed before matching
# Default value 12
sigma=52
# removeDuplicateTokens must be True or False
# If True, duplicate tokens in same reference are removed
# Default value False
removeDuplicateTokens=True
# removeExcludedBlkTokens must be True or False
# If True, tokens excluded from blocking are removed from ref
# Default value True
removeExcludedBlkTokens=False
########################################
# Linking Paramters
# epsilon must be decimal value between 0.0 and 1.0
# only clusters with quality >= epsilon are kept each iteration
# Default value 0.70
epsilon=0.31
# epsilonIterate must be decimal value between 0.0 and 1.0
# value added to epsilon at the end of each iteration
# Default value 0.0
epsilonIterate=0.0
# mu must be decimal value between 0.0 and 1.0
# starting value for match threshold to link two references
# Default value 0.5
mu=0.74
# muIterate must be decimal value between 0.0 and 1.0
# value added to mu at the end of each iteration
# Default value 0.10
muIterate=0.06
# comparator must be the name of a valid comparator
# 'Cosine','MongeElkan','ScoringMatrixStd', 'ScoringMatrixKris', 'TahaComparator'
# Default value ScoringMatrixKris
comparator=ScoringMatrixStd
# matrixNumTokenRule must be True of False
# If True, requires exact match between two numeric tokens
# applies only to ScoringMatrixStd, ScoringMatrixKris, and TahaComparator
# Default value False
matrixNumTokenRule=False
# matrixInitialRule must be True of False
# If True, requires exact match if either token has length 1
# applies only to ScoringMatrixStd, ScoringMatrixKris, and TahaComparator
# Default value False
matrixInitialRule=True
# TahaComparator settings
tahaRejectMuRatio=0.95
tahaReviewUpperDelta=0.05
tahaContextThreshold=0.85
tahaMinNameSimilarity=0.50
tahaMinLastNameSimilarity=0.45
tahaEnableNameHardReject=False
tahaAliasFile=alias.dat
tahaLlmDecisionFile=
tahaUseOpenAIReview=True
tahaOpenAIModel=o3
tahaOpenAIApiKeyEnv=OPENAI_API_KEY
tahaOpenAIBaseURL=https://api.openai.com/v1
tahaOpenAITimeoutSec=45
tahaOpenAIMaxRetries=2
tahaOpenAITemperature=0.0
tahaOpenAIMaxOutputTokens=120
tahaOpenAIReviewMaxPairs=2000
tahaUseAsyncOpenAIReview=True
tahaOpenAIAsyncWorkers=8
tahaUseAnchorBatchReview=True
tahaAnchorBatchSize=6
tahaUseDeterministicRules=True
tahaPoisonAddressMinSimilarity=0.94
tahaPoisonFirstNameMax=0.35
tahaPoisonLastNameMax=0.65
tahaPoisonNameSimilarityMax=0.62
tahaPoisonRequireAddressNumberMatch=True
tahaCoreConflictFirstNameMax=0.20
tahaCoreConflictLastNameMax=0.50
tahaCoreConflictNameSimilarityMax=0.55
tahaStrongNameAcceptNameMin=0.96
tahaStrongNameAcceptLastMin=0.95
tahaStrongNameAcceptFirstMin=0.90
tahaStrongNameAcceptPositionalMin=0.95
tahaStrongNameAcceptMuDelta=0.26
tahaLlmContextMaxExamples=3
tahaPersistCachesAcrossIterations=True
tahaCollectDecisionTraceCount=False
tahaPairFeatureCacheMaxSize=25000
tahaProgressInterval=500
tahaUseSoftRoleScoring=False
tahaUseProvisionalContext=False
tahaUseSchemaLightClusterQuality=False
tahaMustLinkThreshold=0.87
tahaLikelyLinkThreshold=0.74
tahaContextOnlyThreshold=0.58
tahaCannotLinkThreshold=0.28
tahaLocalSupportWeight=0.18
tahaLocalConflictWeight=0.20
tahaSchemaLightRareTokenMaxFreq=8
tahaContextDisagreementDelta=0.12
############################
